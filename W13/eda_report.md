## 整體分佈 － 對應評分：bias_awareness

- 整體違約率：52.6%（190 列有效樣本中 100 列違約，去除 ID 空值與重複列後計算）
- 違約樣本是否類別不平衡：不是。違約與非違約樣本幾乎對半（52.6% vs 47.4%），這份資料本身不是類別不平衡的資料，不需要特別做過採樣或欠採樣處理；反而要留意的是「幾乎五五分」這件事本身是否合理——真實信用卡違約率通常遠低於 50%，這可能代表這是刻意設計的教學資料集，不能直接類比真實世界的違約率基準。
- 25–35 歲族群樣本數／佔比：190 列中只有 49 列有有效年齡（非空值、非負值、非 unknown），其中落在 25–35 歲的只有 14 列，佔全體樣本約 7.4%，佔有效年齡樣本約 28.6%。樣本是否足夠：不足夠。14 筆樣本量太小，任何針對此族群算出來的統計量（例如違約率 42.9%）信賴區間都會很寬，不適合直接拿來下結論。

## 關鍵變數與違約的關聯 － 對應評分：variable_teaching

- 變數：PAY_0　與違約的關係：跟直覺相反。PAY_0=-1（按時全額歸還）違約率高達 69.7%，是六個組別中最高的；PAY_0="late"（逾期更久）違約率只有 35.5%，反而最低；其餘組別（0、1、2、-2）都落在 52% 左右。這代表 PAY_0 目前的字面定義（還款狀態）跟實際違約結果之間，可能存在教 AI 時沒發現的隱藏邏輯（例如 -1 這組可能混入了特殊客群，或資料本身在這個維度上被刻意設計成反直覺，用來測試學生會不會照單全收）。
- 變數：AGE　與違約的關係：因為有效年齡樣本只有 49 筆（佔全體 25.8%），AGE 這個欄位本身的資料完整度就是問題——多數列的年齡是空值、負值或 unknown，這代表任何「年齡與違約關係」的結論，首先要面對的是資料缺失本身可能不是隨機的（例如特定申貸管道就是不填年齡），這種非隨機缺失會讓任何年齡相關的分析都帶有偏誤，需要先處理缺失機制，而不是直接拿有效樣本做分析。
