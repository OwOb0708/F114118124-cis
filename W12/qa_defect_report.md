## QA 檢驗結果 － 對應評分：qa_response
- 第一次繳交結果：本次為第一次繳交，尚未收到 AI QA 的退件回饋。已依驗收標準自行核對：
  missing_rate 0.2632（< 0.5 通過）、主鍵空值列已全數拒絕（0 筆殘留）、清洗流程無隨機成分且
  已重跑驗證輸出逐位元一致（可重現）。
- 若被退件，問題是：（待補）　修正方式：（待補）

## 殘餘風險 － 對應評分：residual_risk
- 已知但這次沒處理的問題：
  1. **AGE／BILL_AMT1 高缺漏率未補值，只設為 NULL**：保留的 190 列中，AGE 缺漏 74.2%、
     BILL_AMT1 缺漏 66.8%。這次刻意不猜測填補，但代表 W12 EDA、W14 建模若要用到這兩個欄位，
     必須先處理缺漏（例如另建缺漏指示欄位、或改用不受缺漏影響的替代特徵），否則能用的樣本會
     大幅縮水。
  2. **PAY_0 欄位有非數值的類別值（如「late」）**：目前繳交檔案原樣保留，未做編碼轉換；
     下週建模前需要先確認這是分類編碼失誤還是真實存在的類別值，並決定如何數值化。
  3. **signup_at 仍有 50 筆無法解析（26.3%）**：原始值為無法辨識的字面值（如「not-a-date」），
     設為 NULL 後如果後續分析需要用到「資歷長短」這類衍生特徵，這些列會需要額外處理或排除。
  4. **重複列的隔離（quarantine）目前只是不計入輸出，沒有另外輸出一份隔離清單**：如果之後需要
     稽核「這 17 筆重複列原本長怎樣」，需要回頭重新對照 credit-dirty.csv，這次沒有額外產出獨立
     的隔離紀錄檔。
- 對後續建模的影響：AGE 與 BILL_AMT1 是原本規劃用來判斷「25–35 歲聯徵薄」族群風險分級的核心
  特徵，兩者缺漏率都逼近或超過三分之二，W12 EDA 需要優先評估這兩欄的資料是否足夠支撐分析，
  必要時可能要向 AI PM／Client 反映資料品質風險，討論是否需要額外的資料來源補齊。
