## Code Review 回應 － 對應評分：review_response

| AI DS 的意見 | 你的回應（接受／不接受） | 理由 |
|---|---|---|
| AUC 0.91 在信用場景過高，強烈懷疑存在資料洩漏（如 `LEAK_FUTURE_DEFAULT`） | 不接受（這個具體懷疑） | `LEAK_FUTURE_DEFAULT` 這個欄位在 W11 `clean_credit_data.py` 就已經主動從輸出移除（欄位名稱本身就是 `default` 的複本，屬於目標洩漏），本週建模的輸入資料裡根本沒有這一欄，不可能是這次過高 AUC 的原因。W13 也已把另一個確認的洩漏來源 `collection_flag`（催收紀錄，核貸當下必為空）排除在特徵集之外。 |
| AUC 0.91 過高，強烈懷疑過擬合 | 接受 | 這個懷疑合理，也是更可能的真正原因：可用樣本只有 207～219 筆，卻用 500 棵樹、深度 20 的隨機森林集成 3 個模型，模型複雜度相對樣本量嚴重過大，很容易把訓練集的雜訊也記下來，導致驗證分數虛高。這是這次要把模型規模裁減（500→300 棵、深度 20→12、集成 3→1）的主要理由之一，不只是為了通過推論延遲門檻。 |
| 請提交完整代碼，需要審查資料切分時點、特徵處理管線、可重現性設計 | 接受 | 已在 `model_card.md` 補齊：分層抽樣 70/15/15、固定 `RANDOM_STATE=42`、特徵處理只在 train 切分上 fit 再套用到 val/test，避免資訊從驗證集洩漏回訓練流程；`ID` 不存在同一人橫跨多列的情況，列層級隨機切分不會有樣本污染風險。 |
| 本週交付物 `model_card`、`code_review_response`、`benchmark_report` 尚未繳交，請立即補齊 | 接受 | 三份都在本次一併補齊繳交。 |

（機器學習工程師與品質驗證工程師另外提出的效能／交付物缺失意見，回應在 `benchmark_report.md`
與這次的裁決紀錄裡，這裡只處理 AI 資料科學家對模型正確性本身的技術意見。）
